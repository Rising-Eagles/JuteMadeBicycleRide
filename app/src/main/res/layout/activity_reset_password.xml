<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_page_reset_password"
    tools:context=".UserRegistration.ResetPasswordActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/field_reset_password_email"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_gravity="center">

            <ImageView
                android:id="@+id/icon_regd_user_reset_password_email"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:padding="2dp"
                android:src="@drawable/reset_password_email"/>

            <EditText
                android:id="@+id/regd_user_reset_password_email"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:background="@drawable/background_inputbox_reset_password"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:hint="Enter your email"
                android:inputType="textEmailAddress"
                android:layout_gravity="center"
                android:textAlignment="center"
                android:textColorHint="@color/ColorGreenDark"
                android:textStyle="italic" />

        </LinearLayout>

        <Button
            android:id="@+id/regd_user_reset_password"
            android:layout_width="match_parent"
            android:layout_height="35dp"
            android:text="Reset Password"
            android:textSize="20dp"
            android:textColor="@color/ColorBlack"
            android:textStyle="bold"
            android:layout_gravity="center"
            android:textAlignment="center"
            android:layout_marginTop="2dp"
            android:background="@drawable/background_button_reset_password"
            android:layout_below="@id/field_reset_password_email"/>

        <ProgressBar
            android:id="@+id/reset_password_progbar"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:layout_marginTop="5dp" />

    </LinearLayout>


</RelativeLayout>